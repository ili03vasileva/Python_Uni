import sys

def unique_elements(lst):
    
    unique_set = set(lst)
    

    unique_list = list(unique_set)
    
    return unique_list

if _name_ == "_main_":
    if len(sys.argv) < 2:
        print("Usage: python FXXXXX_L2_T4.py <list>")
    else:
        input_list = [int(x) for x in sys.argv[1:]]
        result = unique_elements(input_list)
        print(result)
